<p id="notice"><%= notice %></p>

<p>
  <strong>Username:</strong>
  <%= @user.username %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>

<p>
</p>

<h1>Listing proclamations</h1>

<table>
  <thead>
    <tr>
      <th>Proclamation</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <!-- rename proclamation to body (edict) -->

  <tbody>
    <% @user.proclamations.each do |proclamation| %>
      <tr>
        <td><%= proclamation.proclamation %></td>
        <td><%= link_to 'Show', proclamation %></td>
<!--         need to make this authorized
        <td><%= link_to 'Edit', edit_proclamation_path(proclamation) %></td>
        <td><%= link_to 'Destroy', proclamation, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>